{
  "name": "sintu-astro-theme",
  "type": "module",
  "version": "1.0.0",
  "description": "Sintu theme for Astro - 单栏自适应博客主题",
  "author": "Jin",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/post-build-image-processor.js",
    "preview": "npm run build && wrangler dev",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.10",
    "@astrojs/rss": "^4.0.18",
    "astro": "^6.1.8"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
